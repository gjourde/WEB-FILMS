{# A corriger #}
{# Creation Formulaire d'ajout de Film #}
	<div>
		{% if status is not defined %}
			<form action="ajouter_film" method="post"> <div class="container">
				<input type="text" name="ID film">
				<input type="text" name="titre du film">
				<input type="text" name="realisateur">
				<input type="text" name="affiche">
				<input type="text" name="annee">
				<button type="submit">Ajouter le Film</button>
			</div>
		</form>
	{% else %}
		<p>{{status}}
			:</p>
		{% if ajoutFilm is defined %}
			<p>{{ajoutFilm.getTitre}}</p>
			<p>{{ajoutFilm.getRealisateur}}</p>
			<p>{{ajoutFilm.getAffiche}}</p>
			<p>{{ajoutFilm.getAnnee}}</p>
		{% endif %}
	{% endif %}

	<button>
		<a href="index" style="text-decoration : none;">
			Retour</a>
	</button>
</div>
